export default function Foot({currentYear}) {
    //const curentYear = new Date().getFullYear();

    return (
        <footer>
            &copy; {currentYear} CodeCraft Labs. All rights reserved.
        </footer>
    );
}